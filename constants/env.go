package constants

// Environment: one of (dev/development, prod/production)
const Env = "dev"

const (
	DBHosts = "localhost"		// address (host address)
	DBUserName = ""				// database username
	DBPassword = ""				// database password
	DBName = ""					// database name
)